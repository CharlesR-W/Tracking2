# Deprecated LessWrong-post artifacts

These files are measured history, but they are not canonical publication
evidence and must not be added to `../dashboard_manifest.json` without a new
provenance review.

- `prior_diagnostic_different_checkpoint/` contains two rank-gate diagnostics
  from checkpoint SHA-256 prefix `5443f`. The current seed-0 controls use
  checkpoint prefix `6ca7d`, so the old rows must never be pooled or paired.
- `superseded_seed0/cut1_r1024_2048_true_eval.json` and
  `superseded_seed0/cut4_r2048_true_eval.json` are unreferenced intermediate
  seed-0 outputs. They were superseded by the manifest-listed projection and
  control artifacts.
