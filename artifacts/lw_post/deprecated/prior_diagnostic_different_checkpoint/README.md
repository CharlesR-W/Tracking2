# Prior rank-gate diagnostics — different checkpoint

These two measured artifacts came from checkpoint SHA-256 beginning `5443f`.
The current seed-0 controls and training manifest use checkpoint SHA-256
beginning `6ca7d`.

They are retained as diagnostic history, but they are intentionally excluded
from `artifacts/lw_post/dashboard_manifest.json`. Do not pool or pair them with
the current seed-0 evidence.
